export const normalizeQuery=(value:string)=>value.normalize('NFKD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^\p{L}\p{N}]+/gu,' ').trim();export function rankText(query:string,value:string){const q=normalizeQuery(query),v=normalizeQuery(value);if(!q||!v)return 0;if(v===q)return 100;if(v.startsWith(q))return 80;if(v.includes(q))return 60;const tokens=q.split(' ');return tokens.reduce((score,token)=>score+(v.includes(token)?10:0),0);}
