import{describe,expect,it}from'vitest';import{normalizeQuery,rankText}from'../src/discovery/search-ranking';describe('search ranking',()=>{it('normalizes accents and punctuation',()=>expect(normalizeQuery('  Télé-Série! ')).toBe('tele serie'));it('ranks exact and prefix matches first',()=>{expect(rankText('Dune','Dune')).toBeGreaterThan(rankText('Dune','Dune Part Two'));expect(rankText('Dune','The story of Dune')).toBeGreaterThan(0);});});
