import { describe,expect,it } from 'vitest';import { UrlPolicyService } from '../src/sources/url-policy.service';
describe('UrlPolicyService',()=>{const policy=new UrlPolicyService();it('blocks localhost and private IP ranges',async()=>{await expect(policy.validate('http://127.0.0.1/list.m3u',['127.0.0.1'])).rejects.toThrow('SOURCE_PRIVATE_NETWORK_BLOCKED');await expect(policy.validate('http://192.168.1.5/list.m3u',['192.168.1.5'])).rejects.toThrow('SOURCE_PRIVATE_NETWORK_BLOCKED');});it('requires an explicit allowed host',async()=>{await expect(policy.validate('https://8.8.8.8/list.m3u',['provider.example'])).rejects.toThrow('SOURCE_HOST_NOT_ALLOWED');});it('rejects embedded credentials and unsafe ports',async()=>{await expect(policy.validate('https://user:pass@8.8.8.8/list.m3u',['8.8.8.8'])).rejects.toThrow('SOURCE_URL_BLOCKED');await expect(policy.validate('https://8.8.8.8:8080/list.m3u',['8.8.8.8'])).rejects.toThrow('SOURCE_URL_BLOCKED');});});
