import { ConfigService } from '@nestjs/config';import { describe,expect,it } from 'vitest';import { SourceVaultService } from '../src/sources/source-vault.service';
describe('SourceVaultService',()=>{it('encrypts credentials with authenticated encryption',()=>{const vault=new SourceVaultService(new ConfigService({SOURCE_ENCRYPTION_KEY:'k'.repeat(32)}));const encrypted=vault.encrypt({username:'adam',password:'secret',macAddress:'00:1A:79:00:00:01'});expect(encrypted.ciphertext).not.toContain('secret');expect(vault.decrypt(encrypted)).toEqual({username:'adam',password:'secret',macAddress:'00:1A:79:00:00:01'});});it('detects tampering',()=>{const vault=new SourceVaultService(new ConfigService({SOURCE_ENCRYPTION_KEY:'k'.repeat(32)}));const encrypted=vault.encrypt({password:'secret'});expect(()=>vault.decrypt({...encrypted,authTag:Buffer.alloc(16).toString('base64')})).toThrow();});});
