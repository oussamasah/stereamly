import { notFound } from 'next/navigation'; import { Header } from '../../../components/header'; import { CatalogueClient } from '../../../components/catalogue-client'; import { getMessages,isLocale } from '../../../i18n';
export default async function Channels({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();return <main className="shell"><Header locale={locale}/><div className="page-heading"><p className="eyebrow">STREAMLY LIVE</p><h1>{getMessages(locale).channels}</h1></div><CatalogueClient locale={locale} kind="channels"/></main>;}
