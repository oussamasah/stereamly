import{notFound}from'next/navigation';import{Header}from'../../../../../components/header';import{SecurePlayer}from'../../../../../components/secure-player';import{isLocale}from'../../../../../i18n';export default async function Watch({params}:{params:Promise<{locale:string;type:string;id:string}>}){const{locale,type,id}=await params;if(!isLocale(locale)||!['media','episode','channel'].includes(type))notFound();return <main className="shell"><Header locale={locale}/><SecurePlayer targetType={type.toUpperCase()as'MEDIA'|'EPISODE'|'CHANNEL'} targetId={id}/></main>}
