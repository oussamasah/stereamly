import { notFound } from 'next/navigation';
import { AuthForm } from '../../../components/auth-form';
import { Header } from '../../../components/header';
import { isLocale } from '../../../i18n';
import Link from 'next/link';
export default async function Login({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; if (!isLocale(locale)) notFound(); const copy=locale==='en'?{eye:'WELCOME BACK',title:'Your next story is waiting.',lead:'Return to your list, live channels and unfinished episodes.',points:['Continue where you stopped','Your favourites, ready to play','One account across your screens'],form:'Sign in',sub:'Use your Streamly account to continue.',switch:'New to Streamly?',link:'Create an account'}:locale==='ar'?{eye:'مرحبًا بعودتك',title:'قصتك التالية بانتظارك.',lead:'عد إلى قائمتك والقنوات المباشرة والحلقات غير المكتملة.',points:['أكمل من حيث توقفت','مفضلاتك جاهزة للمشاهدة','حساب واحد لكل شاشاتك'],form:'تسجيل الدخول',sub:'استخدم حساب Streamly للمتابعة.',switch:'جديد في Streamly؟',link:'إنشاء حساب'}:{eye:'HEUREUX DE VOUS REVOIR',title:'Votre prochaine histoire vous attend.',lead:'Retrouvez votre liste, le direct et vos épisodes en cours.',points:['Reprenez là où vous étiez','Vos favoris prêts à regarder','Un compte sur tous vos écrans'],form:'Se connecter',sub:'Utilisez votre compte Streamly pour continuer.',switch:'Nouveau sur Streamly ?',link:'Créer un compte'}; return <main className="shell"><Header locale={locale}/><div className="auth-page"><section className="auth-intro"><p className="eyebrow">{copy.eye}</p><h1>{copy.title}</h1><p className="lead">{copy.lead}</p><div className="auth-points">{copy.points.map(point=><span className="auth-point" key={point}>{point}</span>)}</div></section><section className="auth"><h2>{copy.form}</h2><p className="auth-subtitle">{copy.sub}</p><AuthForm locale={locale} mode="login"/><p className="auth-switch">{copy.switch} <Link href={`/${locale}/register`}>{copy.link}</Link></p></section></div></main>; }
