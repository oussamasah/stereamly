import{notFound}from'next/navigation';import{Header}from'../../../../components/header';import{MediaDetail}from'../../../../components/media-detail';import{isLocale}from'../../../../i18n';export default async function Series({params}:{params:Promise<{locale:string;slug:string}>}){const{locale,slug}=await params;if(!isLocale(locale))notFound();return <main><div className="shell"><Header locale={locale}/></div><MediaDetail locale={locale} slug={slug}/></main>}
