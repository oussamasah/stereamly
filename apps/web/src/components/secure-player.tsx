'use client';
import{useEffect,useRef,useState}from'react';const api=process.env.NEXT_PUBLIC_API_URL;type TargetType='MEDIA'|'EPISODE'|'CHANNEL';type StreamType='mpegts'|'hls'|'dash'|'file'|'manifest';
export function SecurePlayer({targetType,targetId}:{targetType:TargetType;targetId:string}){const video=useRef<HTMLVideoElement>(null),[status,setStatus]=useState('Préparation du lecteur…'),[retry,setRetry]=useState(0),[failed,setFailed]=useState(false);useEffect(()=>{let cleanup:(()=>void|Promise<void>)|undefined,heartbeat:ReturnType<typeof setInterval>|undefined,progressTimer:ReturnType<typeof setInterval>|undefined,sessionId:string|undefined,cancelled=false;const token=sessionStorage.getItem('accessToken')??'',authorization=`Bearer ${token}`,deviceId=localStorage.getItem('streamlyDeviceId')??crypto.randomUUID();localStorage.setItem('streamlyDeviceId',deviceId);setFailed(false);setStatus('Préparation du lecteur…');const saveProgress=()=>{const media=video.current;if(!media||targetType==='CHANNEL'||!Number.isFinite(media.currentTime))return;const payload={mediaTitleId:targetType==='MEDIA'?targetId:undefined,episodeId:targetType==='EPISODE'?targetId:undefined,positionSec:Math.max(0,Math.floor(media.currentTime)),durationSec:Number.isFinite(media.duration)?Math.max(1,Math.floor(media.duration)):undefined,completed:Number.isFinite(media.duration)&&media.duration>0&&media.currentTime/media.duration>.92,deviceId};void fetch(`${api}/library/progress`,{method:'POST',headers:{authorization,'content-type':'application/json'},body:JSON.stringify(payload)}).catch(()=>undefined)};void(async()=>{try{const response=await fetch(`${api}/playback/sessions`,{method:'POST',headers:{authorization,'content-type':'application/json'},body:JSON.stringify({targetType,targetId,deviceId})});if(!response.ok){const body=await response.json().catch(()=>({}))as{message?:string|string[]};if(!cancelled){setFailed(true);setStatus(playbackError(response.status,body.message))}return}const value=await response.json()as{sessionId:string;manifestUrl:string;streamType?:StreamType};sessionId=value.sessionId;if(cancelled||!video.current)return;const media=video.current,url=new URL(value.manifestUrl,new URL(api!).origin).toString(),playing=()=>{if(!cancelled){setStatus('');setFailed(false)}},waiting=()=>{if(!cancelled)setStatus('Connexion au flux…')};media.addEventListener('playing',playing);media.addEventListener('waiting',waiting);if(value.streamType==='mpegts'){const mpegts=await import('mpegts.js');if(!mpegts.default.isSupported())throw new Error('MPEGTS_UNSUPPORTED');const created=mpegts.default.createPlayer({type:'mpegts',isLive:true,url},{enableWorker:true,enableStashBuffer:false,liveBufferLatencyChasing:true,liveBufferLatencyMaxLatency:5,liveBufferLatencyMinRemain:1}),onError=()=>{if(!cancelled){setFailed(true);setStatus('Le direct a été interrompu. Réessayez dans quelques secondes.');void qoe(value.sessionId,'error',authorization)}};created.on(mpegts.default.Events.ERROR,onError);cleanup=()=>{media.removeEventListener('playing',playing);media.removeEventListener('waiting',waiting);created.off(mpegts.default.Events.ERROR,onError);created.pause();created.unload();created.detachMediaElement();created.destroy()};created.attachMediaElement(media);created.load();await created.play()}else if(value.streamType==='hls'){if(media.canPlayType('application/vnd.apple.mpegurl')){media.src=url;cleanup=()=>resetMedia(media,playing,waiting);await media.play()}else{const Hls=(await import('hls.js')).default;if(!Hls.isSupported())throw new Error('HLS_UNSUPPORTED');let recoveries=0;const created=new Hls({lowLatencyMode:targetType==='CHANNEL',maxBufferLength:targetType==='CHANNEL'?15:30,maxMaxBufferLength:60,backBufferLength:20,manifestLoadingTimeOut:8_000,fragLoadingTimeOut:10_000,manifestLoadingMaxRetry:3,fragLoadingMaxRetry:3,startFragPrefetch:true}),onError=(_event:string,data:{fatal:boolean;type:string})=>{if(!data.fatal)return;void qoe(value.sessionId,'error',authorization);if(recoveries++<2&&data.type===Hls.ErrorTypes.NETWORK_ERROR){setStatus('Reconnexion au flux…');created.startLoad();return}if(recoveries<3&&data.type===Hls.ErrorTypes.MEDIA_ERROR){setStatus('Récupération de la lecture…');created.recoverMediaError();return}setFailed(true);setStatus('Le flux ne répond plus. Vous pouvez relancer la lecture.')};created.on(Hls.Events.ERROR,onError);cleanup=()=>{media.removeEventListener('playing',playing);media.removeEventListener('waiting',waiting);created.off(Hls.Events.ERROR,onError);created.destroy()};created.loadSource(url);created.attachMedia(media);await media.play().catch(()=>setStatus('Touchez Lecture pour démarrer.'))}}else if(value.streamType==='file'){media.src=url;cleanup=()=>resetMedia(media,playing,waiting);await media.play().catch(()=>setStatus('Touchez Lecture pour démarrer.'))}else{const shaka=await import('shaka-player'),created=new shaka.default.Player();cleanup=()=>{media.removeEventListener('playing',playing);media.removeEventListener('waiting',waiting);return created.destroy()};await created.attach(media);created.configure({streaming:{bufferingGoal:20,rebufferingGoal:3,bufferBehind:30,retryParameters:{maxAttempts:4,baseDelay:500,backoffFactor:2,fuzzFactor:.2,timeout:12_000,stallTimeout:6_000,connectionTimeout:8_000}}});created.addEventListener('buffering',()=>void qoe(value.sessionId,'buffering',authorization));created.addEventListener('error',()=>{if(!cancelled){setFailed(true);setStatus('Lecture interrompue. Relancez le lecteur.')}void qoe(value.sessionId,'error',authorization)});await created.load(url);await media.play().catch(()=>setStatus('Touchez Lecture pour démarrer.'))}heartbeat=setInterval(()=>void fetch(`${api}/playback/sessions/${value.sessionId}/heartbeat`,{method:'POST',headers:{authorization}}).catch(()=>undefined),30_000);if(targetType!=='CHANNEL')progressTimer=setInterval(saveProgress,15_000)}catch{if(!cancelled){setFailed(true);setStatus('Impossible de démarrer ce flux pour le moment.')}}})();return()=>{cancelled=true;if(heartbeat)clearInterval(heartbeat);if(progressTimer)clearInterval(progressTimer);saveProgress();if(sessionId)void fetch(`${api}/playback/sessions/${sessionId}/end`,{method:'POST',headers:{authorization,'content-type':'application/json'},body:'{}'}).catch(()=>undefined);void cleanup?.()}},[targetId,targetType,retry]);return <div className="player-shell"><div className="player-stage"><video ref={video} controls autoPlay playsInline preload="metadata" controlsList="nodownload"/>{status&&<div className={`player-status ${failed?'error':''}`}><span className="player-spinner" aria-hidden="true"/><p aria-live="polite">{status}</p>{failed&&<button className="button" onClick={()=>setRetry(value=>value+1)}>Réessayer</button>}</div>}</div></div>}
function resetMedia(media:HTMLVideoElement,playing:()=>void,waiting:()=>void){media.removeEventListener('playing',playing);media.removeEventListener('waiting',waiting);media.pause();media.removeAttribute('src');media.load()}async function qoe(id:string,type:string,authorization:string){try{await fetch(`${api}/playback/sessions/${id}/qoe`,{method:'POST',headers:{authorization,'content-type':'application/json'},body:JSON.stringify({type})})}catch{/* Playback continues when telemetry is unavailable. */}}
function playbackError(status:number,value?:string|string[]){const code=Array.isArray(value)?value[0]:value;if(status===401)return'Connectez-vous pour regarder ce contenu.';if(code==='CONTENT_NOT_PUBLISHED')return'Ce contenu n’est pas encore publié.';if(code==='CONCURRENT_STREAM_LIMIT_REACHED')return'Trop de lectures sont actives sur ce compte. Fermez un autre lecteur puis réessayez.';if(code==='DEVICE_LIMIT_REACHED')return'La limite d’appareils de ce compte est atteinte.';if(code==='NO_PLAYABLE_VARIANT'||code==='STREAM_REFERENCE_UNAVAILABLE')return'Aucun flux lisible n’est associé à ce contenu.';if(code==='SUBSCRIPTION_ENTITLEMENT_REQUIRED'||code==='ACTIVE_SUBSCRIPTION_REQUIRED')return'Votre offre ne permet pas encore de regarder ce contenu.';return'Ce contenu est momentanément indisponible.'}
